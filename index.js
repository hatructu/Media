/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TabsManager from './src/components/TabsManager';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TabsManager);
